---
phase: 09-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/Recrd.Cli.Tests/DistValidationTests.cs, scripts/validate-pkg.sh]
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04]

must_haves:
  truths:
    - "Test project for distribution validation exists"
    - "Failing test committed for missing distribution assets"
  artifacts:
    - path: "tests/Recrd.Cli.Tests/DistValidationTests.cs"
      provides: "Distribution validation tests"
    - path: "scripts/validate-pkg.sh"
      provides: "Asset verification logic"
---

<objective>
Define the structure and content requirements for distribution artifacts using TDD.
Create a validation script and failing tests that will act as the gate for subsequent packaging implementation.

Purpose: Enforce "Definition of Done" for distribution assets.
Output: Validation script and failing test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package validation script</name>
  <files>scripts/validate-pkg.sh</files>
  <action>
    Create a bash script that takes a package path and validates its contents.
    It must check for:
    - Main executable (recrd or recrd.exe)
    - Existence of the .playwright/ directory
    - Existence of .playwright/package/cli.js
    - Existence of platform-specific node binary in .playwright/node/
    Exit 1 if any part is missing.
  </action>
  <verify>
    <automated>test -f scripts/validate-pkg.sh && chmod +x scripts/validate-pkg.sh</automated>
  </verify>
  <done>Validation script exists and is executable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create distribution validation tests</name>
  <files>tests/Recrd.Cli.Tests/DistValidationTests.cs</files>
  <behavior>
    - Test: Verify that DistValidationTests.CheckPackage_FailsWhenMissingAssets returns failure when run against an empty directory.
    - Test: Verify that it passes when a mock directory with recrd and .playwright is provided.
  </behavior>
  <action>
    Add a new test file in Recrd.Cli.Tests that invokes scripts/validate-pkg.sh (or similar logic) against expected publish paths.
    Commit the tests as failing (since the publish paths don't exist yet).
  </action>
  <verify>
    <automated>DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --filter "DistValidationTests" || echo "Tests failed as expected in red phase"</automated>
  </verify>
  <done>Tests are committed and fail on missing assets</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Build → Package | Untrusted environment might alter package contents |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | scripts/validate-pkg.sh | mitigate | Use checksums or signature verification in future phases; for now, enforce structure |
</threat_model>

<success_criteria>
1. scripts/validate-pkg.sh exists and correctly identifies a valid/invalid package structure.
2. DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --filter "DistValidationTests" fails because the release assets do not exist yet.
</success_criteria>

<output>
After completion, create `.planning/phases/09-distribution/09-01-SUMMARY.md`
</output>
