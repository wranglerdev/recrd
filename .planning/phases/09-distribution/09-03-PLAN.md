---
phase: 09-distribution
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified: [.github/workflows/release.yml]
autonomous: true
requirements: [DIST-02]

must_haves:
  truths:
    - "Release notes are generated automatically"
    - "GitHub Release includes all 4 platform binaries as assets"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Release notes and upload logic"
---

<objective>
Automate the creation of GitHub Releases, including release notes and asset attachment.
The 4 produced artifacts (win-x64, osx-arm64, osx-x64, linux-x64) must be attached.

Purpose: Formalize the release process and provide binary artifacts to users.
Output: Release notes generator and upload logic in release.yml.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-distribution/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add release notes generation</name>
  <files>.github/workflows/release.yml</files>
  <action>
    Add a step in the release workflow to generate release notes.
    Use an action like `softprops/action-gh-release@v2` which can generate notes automatically, or use a separate generator.
    The notes should include the version, source session SHA-256 (if relevant), and a summary of changes from git.
  </action>
  <verify>
    <automated>grep -q "release_notes" .github/workflows/release.yml || grep -q "generate_release_notes" .github/workflows/release.yml</automated>
  </verify>
  <done>Release notes generation step added</done>
</task>

<task type="auto">
  <name>Task 2: Implement release asset upload</name>
  <files>.github/workflows/release.yml</files>
  <action>
    Add a job (e.g. `release`) that runs after all `publish` jobs in the matrix are complete.
    This job must:
    1. Collect all zip/tar.gz files from previous jobs.
    2. Create a GitHub Release for the tag.
    3. Upload all 4 artifacts as release assets.
    Use `softprops/action-gh-release@v2` with `files: dist/*`.
    Ensure the `GITHUB_TOKEN` has `contents: write` permission.
  </action>
  <verify>
    <automated>grep -q "action-gh-release" .github/workflows/release.yml && grep -q "contents: write" .github/workflows/release.yml</automated>
  </verify>
  <done>Release job added with artifact upload and notes integration</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Actions → GitHub Release | Untrusted release notes content; escaping/sanitization |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | S | Release notes | mitigate | Use official action with built-in sanitization |
</threat_model>

<success_criteria>
1. release.yml includes a `release` job with asset upload.
2. The `generate_release_notes` flag is set to true.
3. Proper permissions for the GitHub token are declared.
</success_criteria>

<output>
After completion, create `.planning/phases/09-distribution/09-03-SUMMARY.md`
</output>
