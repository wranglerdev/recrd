---
phase: 09-distribution
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified: [dist/homebrew/recrd.rb, dist/winget/recrd.yaml, README.md, .github/workflows/release.yml]
autonomous: true
requirements: [DIST-03, DIST-04]

must_haves:
  truths:
    - "Homebrew formula exists and passes basic install simulation"
    - "Winget manifest exists and passes validation"
    - "README includes installation instructions for all platforms"
  artifacts:
    - path: "dist/homebrew/recrd.rb"
      provides: "Homebrew formula"
    - path: "dist/winget/recrd.yaml"
      provides: "Winget manifest"
---

<objective>
Create Homebrew and Winget installation manifests and update documentation.
Ensure that the manifests are valid and tested in the release workflow.

Purpose: Provide standard package manager installation paths for users.
Output: Homebrew formula, winget manifest, and updated docs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-distribution/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Homebrew formula</name>
  <files>dist/homebrew/recrd.rb</files>
  <action>
    Create a Homebrew formula template in dist/homebrew/recrd.rb.
    It must define the binary download URLs (placeholder or dynamic from tag) and the install logic:
    1. Unpack tar.gz
    2. Move 'recrd' to 'bin'
    3. Symlink/include the '.playwright' directory next to the binary.
    Add a test job in `release.yml` on `macos-latest` to run `brew install --build-from-source ./dist/homebrew/recrd.rb` and verify `recrd --version`.
  </action>
  <verify>
    <automated>test -f dist/homebrew/recrd.rb && grep -q "class Recrd" dist/homebrew/recrd.rb</automated>
  </verify>
  <done>Homebrew formula exists with installation logic</done>
</task>

<task type="auto">
  <name>Task 2: Create Winget manifest</name>
  <files>dist/winget/recrd.yaml</files>
  <action>
    Create a winget manifest template in dist/winget/recrd.yaml following the standard YAML format.
    Include metadata for `recrd`, version, and download URL for the Windows zip.
    Add a test job in `release.yml` on `windows-latest` to run `winget validate dist/winget/recrd.yaml` if possible, or use a YAML schema check.
  </action>
  <verify>
    <automated>test -f dist/winget/recrd.yaml && grep -q "PackageIdentifier: recrd" dist/winget/recrd.yaml</automated>
  </verify>
  <done>Winget manifest exists and follows standard format</done>
</task>

<task type="auto">
  <name>Task 3: Update documentation</name>
  <files>README.md, docs/INSTALL.md</files>
  <action>
    Update README.md with an "Installation" section.
    Provide one-liners for:
    - Homebrew: `brew install recrd` (with tap instructions)
    - Winget: `winget install recrd`
    - Direct download: links to GitHub Releases
    - Manual: extraction and PATH instructions
    Include a note about the `.playwright` directory requirement.
  </action>
  <verify>
    <automated>grep -q "Installation" README.md && grep -q "brew install" README.md</automated>
  </verify>
  <done>Documentation updated with installation instructions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Manifests → Package Managers | Untrusted URLs; use SHA-256 checksums in manifests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | T | dist/homebrew/recrd.rb | mitigate | Include SHA-256 checksums for each release asset |
</threat_model>

<success_criteria>
1. Homebrew formula and Winget manifest are created.
2. README.md contains accurate installation instructions.
3. release.yml includes validation for Homebrew and Winget (where possible).
</success_criteria>

<output>
After completion, create `.planning/phases/09-distribution/09-04-SUMMARY.md`
</output>
