---
phase: 11-plugin-system
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/Recrd.Cli.Tests/Plugins/PluginDiscoveryTests.cs
  - tests/Recrd.Cli.Tests/Plugins/PluginLoadContextTests.cs
  - tests/Recrd.Cli.Tests/Plugins/VersionGatingTests.cs
  - tests/Recrd.Cli.Tests/Plugins/ExceptionSafetyTests.cs
  - tests/Recrd.Cli.Tests/Plugins/TestFixtures/FakePlugin.csproj
  - tests/Recrd.Cli.Tests/Plugins/TestFixtures/FakeCompiler.cs
  - tests/Recrd.Cli.Tests/Plugins/TestFixtures/FakeThrowingCompiler.cs
  - apps/recrd-cli/Plugins/PluginManager.cs
  - apps/recrd-cli/Plugins/RecrdPluginLoadContext.cs
  - apps/recrd-cli/Plugins/PluginInfo.cs
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - PLUG-03
  - PLUG-04
must_haves:
  truths:
    - "All plugin system tests exist and fail (red phase)"
    - "Test fixture plugin project builds and produces a real DLL with .deps.json"
    - "Production stubs exist so tests compile against them"
  artifacts:
    - path: "tests/Recrd.Cli.Tests/Plugins/PluginDiscoveryTests.cs"
      provides: "Discovery tests for PLUG-01"
      min_lines: 40
    - path: "tests/Recrd.Cli.Tests/Plugins/VersionGatingTests.cs"
      provides: "Version gating tests for PLUG-03"
      min_lines: 30
    - path: "tests/Recrd.Cli.Tests/Plugins/ExceptionSafetyTests.cs"
      provides: "Exception safety tests for PLUG-04"
      min_lines: 30
    - path: "apps/recrd-cli/Plugins/PluginManager.cs"
      provides: "Production stub for PluginManager"
      min_lines: 10
  key_links:
    - from: "tests/Recrd.Cli.Tests/Plugins/PluginDiscoveryTests.cs"
      to: "apps/recrd-cli/Plugins/PluginManager.cs"
      via: "PluginManager.DiscoverPlugins()"
      pattern: "PluginManager.*Discover"
---

<objective>
TDD red phase: Create all failing tests for the plugin system, plus production stubs so they compile. Build a real test fixture plugin project that produces a DLL with `.deps.json` for integration tests.

Purpose: Establishes the test-first contract for all 4 PLUG requirements before any implementation begins. Tests are committed failing on `tdd/phase-11` branch per D-12.
Output: 4 test files, 1 fixture plugin project, production stub files.
</objective>

<execution_context>
@/home/gil/dev/recrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/gil/dev/recrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-plugin-system/11-CONTEXT.md
@.planning/phases/11-plugin-system/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/Recrd.Core/Interfaces/ITestCompiler.cs:
```csharp
public interface ITestCompiler
{
    string TargetName { get; }
    Task<CompilationResult> CompileAsync(Session session, CompilerOptions options);
}
```

From packages/Recrd.Core/Interfaces/IDataProvider.cs:
```csharp
public interface IDataProvider
{
    IAsyncEnumerable<IReadOnlyDictionary<string, string>> StreamAsync(CancellationToken cancellationToken = default);
}
```

From packages/Recrd.Core/Interfaces/IEventInterceptor.cs:
```csharp
public interface IEventInterceptor
{
    ValueTask<RecordedEvent?> InterceptAsync(RecordedEvent evt, CancellationToken cancellationToken = default);
}
```

From packages/Recrd.Core/Interfaces/IAssertionProvider.cs:
```csharp
public interface IAssertionProvider
{
    string AssertionName { get; }
    AssertionStep CreateAssertion(Selector selector, IReadOnlyDictionary<string, string> payload);
}
```

From packages/Recrd.Core/Interfaces/CompilationResult.cs:
```csharp
public sealed record CompilationResult(
    IReadOnlyList<string> GeneratedFiles,
    IReadOnlyList<string> Warnings,
    IReadOnlyDictionary<string, string> DependencyManifest);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixture plugin project and production stubs</name>
  <files>
    tests/Recrd.Cli.Tests/Plugins/TestFixtures/FakePlugin.csproj,
    tests/Recrd.Cli.Tests/Plugins/TestFixtures/FakeCompiler.cs,
    tests/Recrd.Cli.Tests/Plugins/TestFixtures/FakeThrowingCompiler.cs,
    apps/recrd-cli/Plugins/PluginManager.cs,
    apps/recrd-cli/Plugins/RecrdPluginLoadContext.cs,
    apps/recrd-cli/Plugins/PluginInfo.cs
  </files>
  <action>
  Per D-12 (TDD mandate), create:

  1. **FakePlugin.csproj** — A minimal .NET 10 class library under `tests/Recrd.Cli.Tests/Plugins/TestFixtures/` that references `Recrd.Core` via ProjectReference. Set `<EnableDynamicLoading>true</EnableDynamicLoading>` so `dotnet publish` produces `.deps.json`. This is NOT added to recrd.sln — it's built on-demand by test setup. Set `<IsPackable>false</IsPackable>`.

  2. **FakeCompiler.cs** — Implements `ITestCompiler` with `TargetName => "fake-compiler"`. `CompileAsync` returns a `CompilationResult` with one generated file path and no warnings. This is the happy-path test fixture.

  3. **FakeThrowingCompiler.cs** — Implements `ITestCompiler` with `TargetName => "fake-throwing"`. `CompileAsync` throws `InvalidOperationException("Plugin kaboom")`. Used by PLUG-04 exception safety tests.

  4. **PluginManager.cs** — Production stub in `apps/recrd-cli/Plugins/` namespace `Recrd.Cli.Plugins`. Contains:
     - Constructor taking `string pluginsDirectory`
     - `DiscoverPlugins() -> IReadOnlyList<PluginInfo>` — throws `NotImplementedException()`
     - `GetCompilers() -> IReadOnlyList<ITestCompiler>` — throws `NotImplementedException()`
     - `GetDataProviders() -> IReadOnlyList<IDataProvider>` — throws `NotImplementedException()`

  5. **RecrdPluginLoadContext.cs** — Production stub inheriting `AssemblyLoadContext`. Constructor takes `string pluginPath`, calls `base(isCollectible: true)` per D-07. `Load()` override throws `NotImplementedException()`.

  6. **PluginInfo.cs** — Record type: `PluginInfo(string Name, Version? Version, string[] Interfaces, bool Loaded, string? Error)`. This is the data model for `plugins list` output per D-05/D-06.

  Ensure `Recrd.Cli.Tests.csproj` does NOT reference FakePlugin.csproj — tests will build it dynamically using `dotnet publish` in test setup.
  </action>
  <verify>
    <automated>cd /home/gil/dev/recrd && DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build apps/recrd-cli/recrd-cli.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Production stubs compile. FakePlugin.csproj exists and can be published independently. PluginInfo record type defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write all failing tests for PLUG-01 through PLUG-04</name>
  <files>
    tests/Recrd.Cli.Tests/Plugins/PluginDiscoveryTests.cs,
    tests/Recrd.Cli.Tests/Plugins/PluginLoadContextTests.cs,
    tests/Recrd.Cli.Tests/Plugins/VersionGatingTests.cs,
    tests/Recrd.Cli.Tests/Plugins/ExceptionSafetyTests.cs
  </files>
  <behavior>
  **PluginDiscoveryTests.cs** (PLUG-01 per D-01/D-02/D-03):
  - DiscoverPlugins_EmptyDirectory_ReturnsEmptyList
  - DiscoverPlugins_SubdirectoryWithMatchingDll_ReturnsPluginInfo (create temp dir `Recrd.Plugin.Test/Recrd.Plugin.Test.dll` using FakePlugin publish output)
  - DiscoverPlugins_SubdirectoryWithNonMatchingDll_IgnoresIt (dir named `Foo/` with `Bar.dll`)
  - DiscoverPlugins_FlatDllInRoot_IgnoresIt (DLL directly in plugins/ root — old Phase 8 layout, should be ignored per D-01)

  **PluginLoadContextTests.cs** (PLUG-02 per D-07):
  - LoadContext_RecrdCore_ReturnsNull (type unification — Load("Recrd.Core") returns null)
  - LoadContext_PluginAssembly_LoadsFromPath (loads FakePlugin DLL, gets FakeCompiler type via reflection)
  - LoadContext_PluginImplementsITestCompiler_CastSucceeds (loads FakeCompiler, casts to ITestCompiler without InvalidCastException)

  **VersionGatingTests.cs** (PLUG-03 per D-08):
  - IsCompatible_SameMajorVersion_ReturnsTrue (FakePlugin references same Recrd.Core major version as host)
  - IsCompatible_DifferentMajorVersion_ReturnsFalse (needs a modified assembly or metadata mock — use PEReader on FakePlugin DLL and verify gating logic rejects when major version differs)
  - DiscoverPlugins_IncompatiblePlugin_ReturnsPluginInfoWithError (PluginInfo.Loaded=false, Error contains "version mismatch")

  **ExceptionSafetyTests.cs** (PLUG-04 per D-11):
  - CompileWithPluginException_CatchesAndAddsWarning (invoke FakeThrowingCompiler through PluginManager, verify CompilationResult.Warnings contains "[plugin:fake-throwing]")
  - CompileWithPluginException_HostDoesNotCrash (same as above but assert the method completes without throwing)
  - CompileWithPluginException_OtherCompilersStillRun (if multiple compilers, one throws but others complete)
  </behavior>
  <action>
  Create 4 test files under `tests/Recrd.Cli.Tests/Plugins/`. Each test class uses a shared helper `PluginTestFixture` (private helper class or static method at bottom of each file) that:
  1. Creates a temp directory structure matching D-01 layout
  2. Runs `dotnet publish` on FakePlugin.csproj to get real DLLs with .deps.json
  3. Copies output to the temp directory

  For VersionGatingTests, the "incompatible" test can use `PEReader` to read the FakePlugin DLL and confirm version gating logic — since FakePlugin references the current Recrd.Core (compatible), test the rejection path by constructing a PluginManager with a mocked host version (major version + 1) or by patching the gating method to accept a custom host version parameter.

  All tests MUST fail with `NotImplementedException` from the stubs. This is the red phase.

  Per D-12: tests are committed on `tdd/phase-11` branch.
  </action>
  <verify>
    <automated>cd /home/gil/dev/recrd && DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --no-build --filter "FullyQualifiedName~Plugins" 2>&1 | tail -20</automated>
  </verify>
  <done>All plugin tests compile and fail with NotImplementedException. At least 12 failing tests across 4 files. No test passes (red phase). Code committed on tdd/phase-11 branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin directory -> Host process | Untrusted third-party DLLs loaded into CLI process |
| Plugin DLL metadata -> Version check | Metadata could be crafted to bypass version gating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Plugin DLL | accept | User explicitly places DLLs in ~/.recrd/plugins/; no signing in v1 (deferred per CONTEXT.md) |
| T-11-02 | Denial of Service | Plugin CompileAsync | mitigate | try/catch(Exception) around all plugin calls (Plan 04, PLUG-04) |
| T-11-03 | Elevation of Privilege | AssemblyLoadContext | mitigate | Plugins run in isolated ALC with isCollectible:true; Recrd.Core shared via null return in Load() (Plan 02, D-07) |
| T-11-04 | Information Disclosure | Plugin directory scan | mitigate | Scan restricted to ~/.recrd/plugins/ only; no recursive traversal outside subdirectories (D-01) |
| T-11-05 | Tampering | Version metadata | accept | PEReader reads AssemblyReferences; a crafted DLL could lie about version, but user placed it intentionally |
</threat_model>

<verification>
All tests compile against production stubs and fail with NotImplementedException.
`DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --filter "FullyQualifiedName~Plugins"` shows 12+ failures, 0 passes.
</verification>

<success_criteria>
- 12+ failing tests across 4 test files covering PLUG-01 through PLUG-04
- FakePlugin.csproj builds and publishes with .deps.json
- Production stubs (PluginManager, RecrdPluginLoadContext, PluginInfo) compile
- All code committed on tdd/phase-11 branch
</success_criteria>

<output>
After completion, create `.planning/phases/11-plugin-system/11-01-SUMMARY.md`
</output>
