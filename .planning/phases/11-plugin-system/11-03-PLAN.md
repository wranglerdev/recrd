---
phase: 11-plugin-system
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - apps/recrd-cli/Commands/PluginsCommand.cs
  - apps/recrd-cli/Commands/CompileCommand.cs
  - apps/recrd-cli/Program.cs
  - tests/Recrd.Cli.Tests/Commands/PluginsCommandTests.cs
autonomous: true
requirements:
  - PLUG-01
  - PLUG-03
must_haves:
  truths:
    - "recrd plugins list displays plugin name, version, interfaces, and load status"
    - "recrd plugins install prints manual installation guide and exits 0"
    - "recrd compile --target <plugin-target> routes to plugin-provided compiler"
    - "recrd compile --target unknown lists all available targets including plugin ones"
    - "Version-incompatible plugins show clear rejection in plugins list"
  artifacts:
    - path: "apps/recrd-cli/Commands/PluginsCommand.cs"
      provides: "Subdirectory-aware plugins list + helpful install stub"
      min_lines: 50
    - path: "apps/recrd-cli/Commands/CompileCommand.cs"
      provides: "Plugin compiler integration via PluginManager"
      min_lines: 100
    - path: "apps/recrd-cli/Program.cs"
      provides: "PluginManager wired to both PluginsCommand and CompileCommand"
      min_lines: 30
  key_links:
    - from: "apps/recrd-cli/Program.cs"
      to: "apps/recrd-cli/Plugins/PluginManager.cs"
      via: "new PluginManager(pluginsDir)"
      pattern: "new PluginManager"
    - from: "apps/recrd-cli/Commands/CompileCommand.cs"
      to: "apps/recrd-cli/Plugins/PluginManager.cs"
      via: "pluginManager.GetCompilers()"
      pattern: "GetCompilers"
---

<objective>
Wire PluginManager into the CLI: update `plugins list` to show the informative table with load status, update `plugins install` to be a helpful stub exiting 0, and integrate plugin compilers into `CompileCommand`. Also implements version gating UI (PLUG-03 display).

Purpose: The user can see installed plugins, their status, and use plugin-provided compilers via `--target`.
Output: Updated PluginsCommand, CompileCommand, Program.cs, and updated tests.
</objective>

<execution_context>
@/home/gil/dev/recrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/gil/dev/recrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-plugin-system/11-CONTEXT.md
@.planning/phases/11-plugin-system/11-02-SUMMARY.md

<interfaces>
<!-- From PluginManager (implemented in Plan 02) -->
```csharp
internal class PluginManager
{
    public PluginManager(string pluginsDirectory, Version? hostCoreVersionOverride = null);
    public IReadOnlyList<PluginInfo> DiscoverPlugins();
    public IReadOnlyList<ITestCompiler> GetCompilers();
    public IReadOnlyList<IDataProvider> GetDataProviders();
}

public record PluginInfo(string Name, Version? Version, string[] Interfaces, bool Loaded, string? Error);
```

<!-- From existing CLI -->
```csharp
// CliOutput helpers
internal static class CliOutput
{
    public static void WriteInfo(string message);
    public static void WriteError(string message);
    public static void WriteWarning(string message);
    public static void WriteSuccess(string message);
}
```

<!-- Current CompileCommand compiler lookup (to be replaced) -->
```csharp
var compilers = new Dictionary<string, ITestCompiler>(StringComparer.OrdinalIgnoreCase)
{
    ["robot-browser"] = new RobotBrowserCompiler(),
    ["robot-selenium"] = new RobotSeleniumCompiler()
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire PluginManager into Program.cs and CompileCommand</name>
  <files>apps/recrd-cli/Program.cs, apps/recrd-cli/Commands/CompileCommand.cs</files>
  <action>
  **Program.cs** per D-09/D-10:
  - Create `PluginManager` at startup: `var pluginManager = new PluginManager(Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), ".recrd", "plugins"))`
  - Pass it to `PluginsCommand.Create(pluginManager)` and `CompileCommand.Create(pluginManager, verbosityOption, logOutputOption)`
  - Update method signatures accordingly

  **CompileCommand.cs** per D-10:
  - Add `PluginManager? pluginManager` parameter to `Create()` method (nullable for backward compat in tests)
  - In the compile action, build the compilers dictionary as before with `robot-browser` and `robot-selenium`
  - If `pluginManager` is not null, call `pluginManager.GetCompilers()` and add each to the dictionary keyed by `compiler.TargetName` (plugin compilers do NOT override built-ins — built-ins take precedence if name collision)
  - The error message for unknown target already lists `compilers.Keys` — this now automatically includes plugin targets
  - Wrap `pluginManager.GetCompilers()` call in try/catch(Exception) — if plugin discovery itself fails, log warning and continue with built-in compilers only
  </action>
  <verify>
    <automated>cd /home/gil/dev/recrd && DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build apps/recrd-cli/recrd-cli.csproj --no-restore 2>&1 | tail -3</automated>
  </verify>
  <done>Program.cs creates PluginManager and passes it to both commands. CompileCommand merges plugin compilers with built-ins. Unknown target error lists all available targets.</done>
</task>

<task type="auto">
  <name>Task 2: Update PluginsCommand for D-04/D-05/D-06 and update tests</name>
  <files>apps/recrd-cli/Commands/PluginsCommand.cs, tests/Recrd.Cli.Tests/Commands/PluginsCommandTests.cs</files>
  <action>
  **PluginsCommand.cs** — Replace Phase 8 stub entirely per D-03/D-04/D-05/D-06:

  `Create(PluginManager pluginManager)` — accept PluginManager instead of string dir override.

  **`plugins list`** per D-05:
  - Call `pluginManager.DiscoverPlugins()`
  - If empty, print "No plugins installed" and return 0
  - Print "Installed plugins:" header
  - For each `PluginInfo`, format one line: `  {Name,-30} v{Version,-8} {Interfaces,-25} {StatusIcon} {StatusText}`
  - Status per D-06:
    - `Loaded == true`: print checkmark and "loaded"
    - `Loaded == false && Error contains "version mismatch"`: print cross and the Error string
    - `Loaded == false` (other): print cross and "load error" with Error message
  - Use `Console.Out.WriteLine` for the table (not CliOutput — keeps it simple for piping)

  **`plugins install`** per D-04:
  - Change exit code from 1 to 0
  - Print the exact format from D-04:
    ```
    To install {package}:
      1. dotnet publish {package} -c Release --no-self-contained
      2. Copy the publish output to ~/.recrd/plugins/{package}/

    The directory must contain {package}.dll and {package}.deps.json.
    ```

  **PluginsCommandTests.cs** — Update existing tests:
  - `PluginsList_WithAssemblies_PrintsPluginNames` — Update to use PluginManager constructor (pass temp dir). Create subdirectory layout per D-01 instead of flat DLL. May need to use FakePlugin publish output from Plan 01 fixture or create a minimal mock.
  - `PluginsInstall_ExitsWithCode1` — Rename to `PluginsInstall_ExitsWithCode0` and assert exit code 0 per D-04
  - Add test: `PluginsInstall_PrintsInstallationGuide` — verify output contains "dotnet publish" and "Copy the publish output"
  - Add test: `PluginsList_Empty_PrintsNoPlugins` — empty plugins dir
  - Keep `PluginsCommand_HasListSubcommand` and `PluginsCommand_HasInstallSubcommandWithPackageArgument` as-is (structural tests still valid)
  </action>
  <verify>
    <automated>cd /home/gil/dev/recrd && DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --no-restore --filter "FullyQualifiedName~PluginsCommand or FullyQualifiedName~VersionGating" 2>&1 | tail -10</automated>
  </verify>
  <done>plugins list shows informative table with name/version/interfaces/status. plugins install prints guide and exits 0. VersionGatingTests pass (incompatible plugins show rejection message). All updated PluginsCommandTests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin target name -> CompileCommand | Plugin-provided TargetName used in dictionary key; untrusted string |
| Plugin list output -> User | Display of plugin metadata; user trusts output for decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Spoofing | Plugin TargetName collision | mitigate | Built-in compilers take precedence over plugin compilers with same TargetName; plugin cannot override robot-browser/robot-selenium |
| T-11-08 | Denial of Service | pluginManager.GetCompilers() in CompileCommand | mitigate | Wrapped in try/catch; plugin discovery failure falls back to built-in compilers only |
</threat_model>

<verification>
`DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --filter "FullyQualifiedName~PluginsCommand or FullyQualifiedName~VersionGating"` — all pass.
</verification>

<success_criteria>
- `recrd plugins list` shows table with name, version, interfaces, load status per D-05
- Version-incompatible plugins show "version mismatch" per D-06
- `recrd plugins install MyPlugin` prints guide and exits 0 per D-04
- `recrd compile --target fake-compiler` invokes plugin compiler per D-10
- `recrd compile --target unknown` error lists built-in AND plugin targets
- All PluginsCommandTests and VersionGatingTests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-plugin-system/11-03-SUMMARY.md`
</output>
