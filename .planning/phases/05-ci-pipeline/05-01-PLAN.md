---
phase: 05-ci-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
  - CI-06
must_haves:
  truths:
    - "A PR against main runs restore, build, test with per-project coverage gates, and format check"
    - "Dropping any gated project below 90% line coverage fails the build with the project name in the error"
    - "A formatting violation fails the build"
    - "Pushing to a tdd/phase-* branch runs tests without failing the build on test failures"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Complete CI pipeline with coverage gates, TDD red-phase support, and format check"
      contains: "threshold"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/Recrd.Core.Tests"
      via: "dotnet test with --threshold 90"
      pattern: "dotnet test.*Recrd\\.Core\\.Tests.*threshold"
    - from: ".github/workflows/ci.yml"
      to: "tdd/phase-* branch detection"
      via: "continue-on-error conditional"
      pattern: "continue-on-error.*true"
---

<objective>
Enhance the existing CI workflow with per-project coverage gates, TDD red-phase branch handling, and ensure all CI-01/CI-02/CI-03/CI-06 requirements are met.

Purpose: Every PR and push gets a fully automated quality gate that enforces 90% line coverage per gated project and allows TDD red-phase branches to have failing tests without breaking the build.
Output: Updated `.github/workflows/ci.yml` with coverage-gated test steps and conditional TDD handling.
</objective>

<execution_context>
@/home/gil/dev/recrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/gil/dev/recrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-pipeline/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-project coverage gates and TDD red-phase support to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current workflow to extend — must preserve all existing steps)
    - .planning/phases/05-ci-pipeline/05-CONTEXT.md (locked decisions D-01 through D-10)
  </read_first>
  <action>
Rewrite `.github/workflows/ci.yml` preserving the existing structure but enhancing it per the locked decisions. The updated workflow must:

**Trigger expansion (per D-08/D-09/D-10):**
- Keep existing triggers: `push: branches: [main]` and `pull_request: branches: [main]`
- Add trigger: `push: branches: ['tdd/phase-*']` so TDD red-phase branches also run CI

**Environment variable:**
- Add `env: DOTNET_SYSTEM_NET_DISABLEIPV6: '1'` at the workflow level so every `dotnet` command uses IPv4 (currently missing from the existing workflow — per CLAUDE.md mandate)

**Steps (in order):**
1. `actions/checkout@v4` (existing)
2. `Setup .NET 10` via `actions/setup-dotnet@v4` with `dotnet-version: '10.0.x'` (existing)
3. `Restore dependencies` — `dotnet restore` (existing)
4. `Build` — `dotnet build --no-restore` (existing)
5. `Assert Recrd.Core has zero Recrd.* references` (existing, unchanged)
6. **Test (non-TDD branches)** — new step with `if: ${{ !startsWith(github.ref_name, 'tdd/phase-') }}`:
   Run `dotnet test --no-build --filter "Category!=Integration"` (no coverage here — coverage is checked per-project in steps 7-10)
7. **Test (TDD red-phase branches)** — new step with `if: ${{ startsWith(github.ref_name, 'tdd/phase-') }}` and `continue-on-error: true`:
   Run `dotnet test --no-build --filter "Category!=Integration"` — test failures do NOT fail the build per D-08
8. **Coverage gate: Recrd.Core.Tests** — per D-01/D-02/D-03:
   ```
   dotnet test tests/Recrd.Core.Tests/Recrd.Core.Tests.csproj --no-build --collect:"XPlat Code Coverage" --threshold 90 --threshold-type line --threshold-stat minimum
   ```
   Name: `Coverage gate — Recrd.Core (90% line)`
9. **Coverage gate: Recrd.Data.Tests** — same pattern:
   ```
   dotnet test tests/Recrd.Data.Tests/Recrd.Data.Tests.csproj --no-build --collect:"XPlat Code Coverage" --threshold 90 --threshold-type line --threshold-stat minimum
   ```
   Name: `Coverage gate — Recrd.Data (90% line)`
10. **Coverage gate: Recrd.Gherkin.Tests** — same pattern:
    ```
    dotnet test tests/Recrd.Gherkin.Tests/Recrd.Gherkin.Tests.csproj --no-build --collect:"XPlat Code Coverage" --threshold 90 --threshold-type line --threshold-stat minimum
    ```
    Name: `Coverage gate — Recrd.Gherkin (90% line)`
11. **Coverage gate: Recrd.Compilers.Tests** — same pattern:
    ```
    dotnet test tests/Recrd.Compilers.Tests/Recrd.Compilers.Tests.csproj --no-build --collect:"XPlat Code Coverage" --threshold 90 --threshold-type line --threshold-stat minimum
    ```
    Name: `Coverage gate — Recrd.Compilers (90% line)`
12. `Check code style` — `dotnet format --verify-no-changes` (existing, unchanged — per D-09 this also runs on tdd/phase-* branches)

**Important notes:**
- The coverlet `--threshold` flags are passed directly to `dotnet test` — coverlet.collector picks them up. The Coverlet NuGet package in the test projects handles the threshold enforcement.
- Coverage gates run on ALL branches including `tdd/phase-*` (per D-09) — only the test step gets `continue-on-error`.
- Remove the old combined `Test` step (line 36 in current file) — it is replaced by the two conditional test steps and the four per-project coverage gates.
  </action>
  <verify>
    <automated>grep -c "threshold" /home/gil/dev/recrd/.github/workflows/ci.yml | grep -q "4" && grep -c "continue-on-error" /home/gil/dev/recrd/.github/workflows/ci.yml | grep -q "1" && grep -q "tdd/phase-" /home/gil/dev/recrd/.github/workflows/ci.yml && grep -q "DOTNET_SYSTEM_NET_DISABLEIPV6" /home/gil/dev/recrd/.github/workflows/ci.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains exactly 4 lines with `--threshold 90`
    - `.github/workflows/ci.yml` contains `continue-on-error: true` exactly once
    - `.github/workflows/ci.yml` contains `startsWith(github.ref_name, 'tdd/phase-')` (at least 2 occurrences — one for TDD test step, one for non-TDD test step)
    - `.github/workflows/ci.yml` contains `DOTNET_SYSTEM_NET_DISABLEIPV6` at workflow-level env
    - `.github/workflows/ci.yml` contains step names: `Coverage gate — Recrd.Core`, `Coverage gate — Recrd.Data`, `Coverage gate — Recrd.Gherkin`, `Coverage gate — Recrd.Compilers`
    - `.github/workflows/ci.yml` contains `dotnet format --verify-no-changes` (unchanged)
    - `.github/workflows/ci.yml` triggers on `push` to `main` and `tdd/phase-*`, and `pull_request` to `main`
    - The old combined `dotnet test --no-build --collect:"XPlat Code Coverage" --filter "Category!=Integration"` step is removed (replaced by conditional steps + per-project gates)
  </acceptance_criteria>
  <done>
    ci.yml has: IPv4 env, 2 conditional test steps (TDD/non-TDD), 4 per-project coverage gates at 90% line threshold, format check. TDD branches get continue-on-error on tests only. All existing steps preserved.
  </done>
</task>

</tasks>

<verification>
- `cat .github/workflows/ci.yml` shows complete workflow with all steps
- YAML syntax is valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`
- Coverage threshold pattern appears 4 times (one per gated project)
- TDD branch conditional appears in trigger and step conditions
- Format check step still present
</verification>

<success_criteria>
1. ci.yml triggers on push to main and tdd/phase-*, and on PRs to main
2. Four separate coverage gate steps each enforce 90% line coverage
3. TDD red-phase branches run tests with continue-on-error: true
4. All other steps (build, core isolation, format) enforce on all branches
5. YAML is syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-pipeline/05-01-SUMMARY.md`
</output>
