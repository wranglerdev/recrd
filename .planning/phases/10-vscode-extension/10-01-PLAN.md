---
phase: 10-vscode-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/vscode-extension/package.json
  - apps/vscode-extension/tsconfig.json
  - apps/vscode-extension/src/extension.ts
  - apps/vscode-extension/.vscode/launch.json
  - apps/vscode-extension/.vscode/tasks.json
  - apps/vscode-extension/esbuild.js
autonomous: true
requirements: [VSCE-05, VSCE-07]

must_haves:
  truths:
    - "Extension activates and displays 'recrd' in the Status Bar"
    - "Clicking the Status Bar item triggers a placeholder command"
    - "Configuration 'recrd.executablePath' is available in VS Code settings"
  artifacts:
    - path: "apps/vscode-extension/package.json"
      provides: "Extension manifest with recrd.path setting and commands"
    - path: "apps/vscode-extension/src/extension.ts"
      provides: "Extension activation and Status Bar initialization"
    - path: "apps/vscode-extension/esbuild.js"
      provides: "Fast build pipeline for the extension"
  key_links:
    - from: "apps/vscode-extension/package.json"
      to: "apps/vscode-extension/src/extension.ts"
      via: "main entry point and activationEvents"
---

<objective>
Scaffold the 'recrd' VS Code extension and implement the Status Bar item for recording state visualization.

Purpose: Establishes the foundational UI and configuration for the extension.
Output: A buildable extension that shows a status bar item and has a configurable CLI path.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vscode-extension/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Extension and Build Pipeline</name>
  <files>apps/vscode-extension/package.json, apps/vscode-extension/tsconfig.json, apps/vscode-extension/esbuild.js</files>
  <action>
    Initialize the extension project in apps/vscode-extension.
    - Create package.json with vscode engine ^1.85.0, extension metadata, and basic scripts (compile, watch, package).
    - Configure esbuild.js for fast bundling of the extension.
    - Setup tsconfig.json for VS Code extension development.
    - Add 'recrd.executablePath' configuration setting with a default value of 'recrd'.
    - Install dependencies: @types/vscode, esbuild, @vscode/webview-ui-toolkit (as dev/prod deps as appropriate).
  </action>
  <verify>
    <automated>cd apps/vscode-extension && npm install && node esbuild.js</automated>
  </verify>
  <done>Extension project is scaffolded and builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Status Bar and Activation</name>
  <files>apps/vscode-extension/src/extension.ts, apps/vscode-extension/src/statusBar.ts</files>
  <action>
    Implement the main extension entry point and Status Bar management.
    - Create src/extension.ts with activate() and deactivate() functions.
    - Implement StatusBarManager class in src/statusBar.ts to create and update a StatusBarItem.
    - Status Bar item should show '$(record) recrd: Idle' by default and have a tooltip.
    - Register a placeholder command 'recrd.showInfo' linked to the Status Bar click.
    - Per VSCE-05: Ensure it can transition between recording, paused, and idle states (even if manually triggered for now).
  </action>
  <verify>
    <automated>cd apps/vscode-extension && node esbuild.js && grep -r "recrd.showInfo" dist/</automated>
  </verify>
  <done>Extension activates, shows Status Bar item, and registers a test command.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Extension scaffold and Status Bar UI</what-built>
  <how-to-verify>
    1. Open the project in VS Code.
    2. Go to 'Run and Debug' and start 'Extension' (requires .vscode/launch.json).
    3. In the [Extension Development Host], verify 'recrd' status bar item exists in the bottom left.
    4. Click the status bar item and verify it shows a notification.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| VS Code Config → Process | User-defined executable path is used to spawn processes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | recrd.executablePath | mitigate | Validate that the path exists and is executable before spawning. |
</threat_model>

<success_criteria>
1. 'apps/vscode-extension' builds successfully using esbuild.
2. Status bar item displays and responds to clicks.
3. 'recrd.executablePath' setting is registered in package.json.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vscode-extension/10-01-SUMMARY.md`
</output>
