---
phase: 10-vscode-extension
plan: 03
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - apps/vscode-extension/src/commands/compile.ts
  - apps/vscode-extension/src/extension.ts
  - apps/vscode-extension/package.json
autonomous: true
requirements: [VSCE-02, VSCE-03]

must_haves:
  truths:
    - "Running 'recrd: Compile' shows a QuickPick for --target"
    - "User can select a data file (CSV/JSON) via file dialog for compilation"
    - "Compiler command successfully invokes 'recrd compile' with selected options"
  artifacts:
    - path: "apps/vscode-extension/src/commands/compile.ts"
      provides: "recrd.compile command with QuickPick and OpenDialog logic"
  key_links:
    - from: "apps/vscode-extension/src/commands/compile.ts"
      to: "apps/vscode-extension/src/cli/runner.ts"
      via: "CliRunner.run() for compilation"
---

<objective>
Implement the compilation command with user-friendly selectors for targets and data files.

Purpose: Enables one-click compilation of recorded sessions from VS Code.
Output: Integrated compilation flow using VS Code native pickers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vscode-extension/RESEARCH.md
@apps/vscode-extension/src/cli/runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Compiler Target QuickPick</name>
  <files>apps/vscode-extension/src/commands/compile.ts</files>
  <action>
    Implement target selection for compilation.
    - Create 'recrd.compile' command handler.
    - Use vscode.window.showQuickPick to let user choose between: 'robot-browser', 'robot-selenium', 'gherkin'.
    - Use the selected value for the '--target' flag in the CLI call.
  </action>
  <verify>
    <automated>cd apps/vscode-extension && node esbuild.js && grep "showQuickPick" dist/</automated>
  </verify>
  <done>User can select compiler target via UI.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Data File Picker</name>
  <files>apps/vscode-extension/src/commands/compile.ts</files>
  <action>
    Implement data file selection.
    - After target selection, ask if user wants to use a data file.
    - If yes, use vscode.window.showOpenDialog to select a .csv or .json file.
    - Use the selected path for the '--data' flag.
    - Also prompt for a .recrd session file if none is active or selected in explorer.
  </action>
  <verify>
    <automated>cd apps/vscode-extension && node esbuild.js && grep "showOpenDialog" dist/</automated>
  </verify>
  <done>User can select data files via native file dialog.</done>
</task>

<task type="auto">
  <name>Task 3: Execute Compilation</name>
  <files>apps/vscode-extension/src/commands/compile.ts, apps/vscode-extension/src/extension.ts</files>
  <action>
    Wire everything to the CLI.
    - Construct the full 'recrd compile' command with all selected flags.
    - Call CliRunner.run() (waiting for exit).
    - Show an information notification on success or error notification on failure.
    - Register command in package.json and extension.ts.
  </action>
  <verify>
    <automated>grep "recrd.compile" apps/vscode-extension/package.json</automated>
  </verify>
  <done>Compilation can be triggered with custom parameters from the editor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Compiler integration with pickers</what-built>
  <how-to-verify>
    1. Start Extension Development Host.
    2. Open a .recrd file or select it in Explorer.
    3. Run 'recrd: Compile Session'.
    4. Verify QuickPick shows for target.
    5. Verify file picker shows for data (if chosen).
    6. Verify CLI runs and produces output files.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System → Shell | Passing file paths to CLI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | CLI Paths | mitigate | Use VS Code API for path selection and wrap paths in quotes when passing to shell. |
</threat_model>

<success_criteria>
1. Compiler options are selectable via VS Code UI.
2. CLI is invoked with correct flags for target and data.
3. User is notified of compilation result.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vscode-extension/10-03-SUMMARY.md`
</output>
