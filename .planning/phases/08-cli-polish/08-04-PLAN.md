---
phase: 08-cli-polish
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - apps/recrd-cli/Commands/VersionCommand.cs
  - apps/recrd-cli/Commands/RecoverCommand.cs
  - apps/recrd-cli/Commands/PluginsCommand.cs
  - apps/recrd-cli/recrd-cli.csproj
autonomous: true
requirements:
  - CLI-06
  - CLI-07
  - CLI-08
  - CLI-12

must_haves:
  truths:
    - "recrd version prints assembly version and .NET runtime version, exits 0"
    - "recrd recover finds the newest .recrd.partial file and reconstructs a session"
    - "recrd plugins list scans ~/.recrd/plugins/ and displays found assemblies"
    - "recrd plugins install <pkg> prints a placeholder message directing to manual install"
    - "All CLI tests pass (TDD green phase)"
    - "dotnet run --project apps/recrd-cli -- version completes in under 500 ms"
  artifacts:
    - path: "apps/recrd-cli/Commands/VersionCommand.cs"
      provides: "Version command printing assembly + runtime info"
      contains: "GetEntryAssembly"
    - path: "apps/recrd-cli/Commands/RecoverCommand.cs"
      provides: "Recover command finding newest .recrd.partial"
      contains: "recrd.partial"
    - path: "apps/recrd-cli/Commands/PluginsCommand.cs"
      provides: "Plugin management stubs scanning ~/.recrd/plugins/"
      contains: "plugins"
  key_links:
    - from: "apps/recrd-cli/Commands/RecoverCommand.cs"
      to: "packages/Recrd.Core/Interfaces/IRecorderEngine.cs"
      via: "RecoverAsync"
      pattern: "RecoverAsync"
---

<objective>
Implement the remaining utility commands (version, recover, plugins), add PublishReadyToRun for cold-start performance, and bring all tests to green (TDD green phase).

Purpose: Completes the CLI command surface. Version provides diagnostics. Recover enables crash recovery. Plugins list/install are stubs for Phase 11. Green phase confirms all tests pass and coverage meets the 90% gate.

Output: All 11 CLI commands fully implemented. All tests green. Coverage gate satisfied. Cold-start target achievable.
</objective>

<execution_context>
@/home/gil/dev/recrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/gil/dev/recrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-polish/08-CONTEXT.md
@.planning/phases/08-cli-polish/08-RESEARCH.md
@.planning/phases/08-cli-polish/08-01-SUMMARY.md
@.planning/phases/08-cli-polish/08-02-SUMMARY.md
@.planning/phases/08-cli-polish/08-03-SUMMARY.md

<interfaces>
<!-- Key types for utility commands -->

From packages/Recrd.Core/Interfaces/IRecorderEngine.cs:
```csharp
public interface IRecorderEngine : IAsyncDisposable
{
    Task<Session> RecoverAsync(string partialPath, CancellationToken cancellationToken = default);
}
```

From packages/Recrd.Recording (concrete implementation):
```csharp
public sealed class PlaywrightRecorderEngine : IRecorderEngine
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VersionCommand + RecoverCommand + PluginsCommand implementation</name>
  <files>
    apps/recrd-cli/Commands/VersionCommand.cs,
    apps/recrd-cli/Commands/RecoverCommand.cs,
    apps/recrd-cli/Commands/PluginsCommand.cs
  </files>
  <read_first>
    apps/recrd-cli/Commands/VersionCommand.cs,
    apps/recrd-cli/Commands/RecoverCommand.cs,
    apps/recrd-cli/Commands/PluginsCommand.cs,
    apps/recrd-cli/Output/CliOutput.cs,
    apps/recrd-cli/Logging/LoggingSetup.cs,
    apps/recrd-cli/Program.cs,
    packages/Recrd.Core/Interfaces/IRecorderEngine.cs,
    packages/Recrd.Recording/PlaywrightRecorderEngine.cs
  </read_first>
  <action>
    **Update `apps/recrd-cli/Commands/VersionCommand.cs`** (per CLI-07):

    Replace stub SetAction with:
    1. Get assembly version: `Assembly.GetEntryAssembly()?.GetName().Version?.ToString() ?? "unknown"`
    2. Get .NET runtime version: `Environment.Version.ToString()`
    3. Get OS description: `System.Runtime.InteropServices.RuntimeInformation.OSDescription`
    4. Print to stdout:
       ```
       recrd {version}
       .NET {runtimeVersion}
       OS {osDescription}
       ```
    5. Return 0

    No logging setup needed for version — it should be as fast as possible for CLI-12.

    **Update `apps/recrd-cli/Commands/RecoverCommand.cs`** (per CLI-06):

    Replace stub SetAction with:
    1. Parse optional `--partial-file` (FileInfo?) from ParseResult
    2. Resolve verbosity/logOutput, create ILoggerFactory
    3. If `--partial-file` provided: use that path directly
    4. If not provided: scan current directory for `*.recrd.partial` files:
       ```csharp
       var partials = Directory.GetFiles(".", "*.recrd.partial")
           .Select(f => new FileInfo(f))
           .OrderByDescending(f => f.LastWriteTimeUtc)
           .ToArray();
       ```
    5. If no partial files found: `CliOutput.WriteError("No .recrd.partial files found in the current directory. Use --partial-file to specify a path.")`, return 1
    6. Use the newest: `var partialPath = partials[0].FullName`
    7. Log which file is being recovered: `logger.LogInformation("Recovering session from {Path}", partialPath)`
    8. Create `PlaywrightRecorderEngine` and call `RecoverAsync`:
       ```csharp
       await using var engine = new PlaywrightRecorderEngine(logger);
       var session = await engine.RecoverAsync(partialPath, ct);
       ```
       NOTE: Check the actual PlaywrightRecorderEngine constructor signature by reading the source. It may require different parameters.
    9. Determine output path: same directory as partial, with `.recrd` extension (strip `.partial` suffix)
    10. Serialize recovered session:
        ```csharp
        var json = JsonSerializer.Serialize(session, RecrdJsonContext.Default.Session);
        await File.WriteAllTextAsync(outputPath, json, new UTF8Encoding(false), ct);
        ```
    11. Print: `CliOutput.WriteSuccess($"Session recovered: {outputPath}")`, return 0

    **Update `apps/recrd-cli/Commands/PluginsCommand.cs`** (per CLI-08):

    Replace stub SetAction for both `list` and `install` subcommands:

    **plugins list:**
    1. Resolve verbosity/logOutput, create ILoggerFactory
    2. Determine plugins directory: `Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), ".recrd", "plugins")`
    3. If directory doesn't exist: `Console.WriteLine("No plugins installed.")`, return 0
    4. Scan for `.dll` files: `Directory.GetFiles(pluginsDir, "*.dll")`
    5. If no DLLs found: `Console.WriteLine("No plugins installed.")`, return 0
    6. For each DLL: print the filename (without extension) as the plugin name
       ```
       Installed plugins:
         - {pluginName1}
         - {pluginName2}
       ```
    7. Return 0

    NOTE: This is a basic directory scan only. No AssemblyLoadContext loading (that is Phase 11). Just list what DLLs are present.

    **plugins install:**
    1. Parse `<package>` argument (string)
    2. Print: `CliOutput.WriteInfo($"Plugin installation is not yet implemented. To install a plugin manually, place the plugin DLL in {pluginsDir}")`, return 1
    3. This is a stub — full plugin installation with NuGet resolution is Phase 11 scope.
  </action>
  <verify>
    <automated>DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build apps/recrd-cli/ --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/recrd-cli/Commands/VersionCommand.cs contains `GetEntryAssembly` and `Environment.Version`
    - apps/recrd-cli/Commands/VersionCommand.cs contains `RuntimeInformation.OSDescription`
    - apps/recrd-cli/Commands/RecoverCommand.cs contains `*.recrd.partial`
    - apps/recrd-cli/Commands/RecoverCommand.cs contains `OrderByDescending`
    - apps/recrd-cli/Commands/RecoverCommand.cs contains `RecoverAsync`
    - apps/recrd-cli/Commands/RecoverCommand.cs contains `UTF8Encoding(` with `false` parameter
    - apps/recrd-cli/Commands/PluginsCommand.cs contains `.recrd", "plugins"` or equivalent path construction
    - apps/recrd-cli/Commands/PluginsCommand.cs contains `No plugins installed`
    - apps/recrd-cli/Commands/PluginsCommand.cs contains `Plugin installation is not yet implemented`
    - `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build apps/recrd-cli/ --no-restore` exits 0
  </acceptance_criteria>
  <done>Version prints assembly + runtime info. Recover finds newest .recrd.partial and calls RecoverAsync. Plugins list scans ~/.recrd/plugins/ for DLLs. Plugins install is a stub with manual install instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Green phase — fix all tests + cold-start ReadyToRun + coverage verification</name>
  <files>
    apps/recrd-cli/recrd-cli.csproj,
    tests/Recrd.Cli.Tests/Commands/StartCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/SessionControlCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/CompileCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/ValidateCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/SanitizeCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/RecoverCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/VersionCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/PluginsCommandTests.cs,
    tests/Recrd.Cli.Tests/Ipc/SessionSocketTests.cs,
    tests/Recrd.Cli.Tests/Ipc/SessionClientTests.cs,
    tests/Recrd.Cli.Tests/Logging/LoggingSetupTests.cs
  </files>
  <read_first>
    apps/recrd-cli/recrd-cli.csproj,
    apps/recrd-cli/Program.cs,
    apps/recrd-cli/Commands/StartCommand.cs,
    apps/recrd-cli/Commands/SessionControlCommand.cs,
    apps/recrd-cli/Commands/CompileCommand.cs,
    apps/recrd-cli/Commands/ValidateCommand.cs,
    apps/recrd-cli/Commands/SanitizeCommand.cs,
    apps/recrd-cli/Commands/RecoverCommand.cs,
    apps/recrd-cli/Commands/VersionCommand.cs,
    apps/recrd-cli/Commands/PluginsCommand.cs,
    apps/recrd-cli/Ipc/SessionSocket.cs,
    apps/recrd-cli/Ipc/SessionClient.cs,
    apps/recrd-cli/Ipc/IpcMessage.cs,
    apps/recrd-cli/Logging/LoggingSetup.cs,
    apps/recrd-cli/Output/CliOutput.cs,
    tests/Recrd.Cli.Tests/Commands/StartCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/SessionControlCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/CompileCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/ValidateCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/SanitizeCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/RecoverCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/VersionCommandTests.cs,
    tests/Recrd.Cli.Tests/Commands/PluginsCommandTests.cs,
    tests/Recrd.Cli.Tests/Ipc/SessionSocketTests.cs,
    tests/Recrd.Cli.Tests/Ipc/SessionClientTests.cs,
    tests/Recrd.Cli.Tests/Logging/LoggingSetupTests.cs
  </read_first>
  <action>
    **Part A: Add PublishReadyToRun to recrd-cli.csproj** (per CLI-12):

    Add to the `<PropertyGroup>` in `apps/recrd-cli/recrd-cli.csproj`:
    ```xml
    <PublishReadyToRun>true</PublishReadyToRun>
    ```

    This enables ReadyToRun (R2R) compilation for published builds, significantly reducing JIT cold-start time. Combined with .NET 10 standard startup (~70-80 ms), this should comfortably meet the 500 ms target.

    Do NOT add NativeAOT or `PublishAot=true` — Playwright's reflection-heavy internals are incompatible. R2R is the correct approach for Phase 8.

    Do NOT add a DI container — manual construction keeps cold start fast (per Pitfall 4 from RESEARCH.md).

    **Part B: Update all test files from red to green:**

    Read every test file created in Plan 01 (Task 1). Replace every `Assert.True(false, ...)` / `Assert.Fail(...)` with real test logic that exercises the production code implemented in Plans 02 and 03.

    The tests must use the **InvocationConfiguration.Output = StringWriter** pattern (from RESEARCH.md Pattern 5) for testing command output without subprocess invocation:
    ```csharp
    var output = new StringWriter();
    var config = new InvocationConfiguration { Output = output };
    var result = rootCommand.Parse(["version"]);
    int exitCode = await result.InvokeAsync(config);
    Assert.Equal(0, exitCode);
    Assert.Contains("recrd", output.ToString());
    ```

    For tests requiring IRecorderEngine or ITestCompiler: use `Moq` to mock the interface. Do NOT start real browsers or compilers in unit tests.

    **Test-by-test guidance:**

    **LoggingSetupTests** (4 tests):
    - Call `LoggingSetup.Create("quiet", false)` and verify the returned ILoggerFactory filters at LogLevel.Error
    - Same for "normal" -> LogLevel.Information, "detailed" -> LogLevel.Debug, "diagnostic" -> LogLevel.Trace
    - Verify by creating a logger and checking `logger.IsEnabled(LogLevel.X)` for various levels

    **VersionCommandTests** (1+ tests):
    - Parse `["version"]` via rootCommand, invoke with StringWriter output, verify exit code 0, verify output contains "recrd"

    **ValidateCommandTests** (2+ tests):
    - Create a temp .recrd file with valid JSON session -> invoke validate -> exit 0
    - Create a temp file with invalid JSON -> invoke validate -> exit 1, stderr contains error

    **SanitizeCommandTests** (2+ tests):
    - Create a temp .recrd file with a session containing ActionStep with literal Payload -> invoke sanitize -> verify output file exists at .sanitized.recrd -> verify output Payload is empty dict and Selector.Values contain "***"

    **SessionSocketTests** (2+ tests):
    - Start SessionSocket in background task -> connect client -> send `{"command":"stop"}` -> verify server loop exits and socket file is deleted
    - Verify `IsSessionActiveAsync` returns false when no socket file exists

    **SessionClientTests** (2+ tests):
    - Call `SendCommandAsync` when no socket file exists -> verify returns 1
    - Start a test socket server -> call `SendCommandAsync("pause")` -> verify message received

    **StartCommandTests** (3+ tests):
    - Mock IRecorderEngine; verify start with default options constructs RecorderOptions with BrowserEngine="chromium"
    - Verify start when session.sock exists returns exit code 1 (create the file first, then clean up)

    **SessionControlCommandTests** (3+ tests):
    - Verify pause/resume/stop call SessionClient with correct command string

    **CompileCommandTests** (3+ tests):
    - Create a valid temp .recrd file -> invoke compile with --target robot-browser -> verify CompileAsync was called on mock compiler

    **RecoverCommandTests** (2+ tests):
    - Create temp .recrd.partial file -> invoke recover -> verify RecoverAsync called with correct path
    - No .recrd.partial files -> verify exit code 1

    **PluginsCommandTests** (2+ tests):
    - Empty ~/.recrd/plugins/ -> plugins list -> "No plugins installed"
    - plugins install -> "not yet implemented"

    **Part C: Run full test suite and verify:**
    ```bash
    DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --no-build --logger "console;verbosity=minimal"
    ```
    All tests must pass. Then run coverage:
    ```bash
    DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --no-build /p:CollectCoverage=true /p:CoverletOutputFormat=cobertura /p:Include="[recrd]*" /p:Threshold=90 /p:ThresholdType=line /p:ThresholdStat=minimum
    ```
    Coverage must meet 90% threshold.

    **Part D: Merge tdd/phase-08 to gsd/phase-08-cli-polish** (if on TDD branch).
  </action>
  <verify>
    <automated>DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --no-build --logger "console;verbosity=minimal" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/recrd-cli/recrd-cli.csproj contains `<PublishReadyToRun>true</PublishReadyToRun>`
    - apps/recrd-cli/recrd-cli.csproj does NOT contain `PublishAot`
    - All test files no longer contain `Assert.True(false` or `Assert.Fail("Red`
    - tests/Recrd.Cli.Tests/Logging/LoggingSetupTests.cs contains `IsEnabled`
    - tests/Recrd.Cli.Tests/Commands/VersionCommandTests.cs contains `InvocationConfiguration` or `StringWriter`
    - tests/Recrd.Cli.Tests/Commands/ValidateCommandTests.cs creates temp .recrd file
    - tests/Recrd.Cli.Tests/Commands/SanitizeCommandTests.cs verifies `sanitized.recrd` output
    - tests/Recrd.Cli.Tests/Ipc/SessionSocketTests.cs contains `UnixDomainSocketEndPoint` or `Socket`
    - `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --no-build` exits 0 (all tests pass)
    - `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --no-build /p:CollectCoverage=true /p:Include="[recrd]*" /p:Threshold=90 /p:ThresholdType=line /p:ThresholdStat=minimum` exits 0 (coverage met)
  </acceptance_criteria>
  <done>All CLI tests green. Coverage >= 90%. PublishReadyToRun enabled for cold-start. Full command surface verified. TDD green phase complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin directory scan | ~/.recrd/plugins/ DLLs listed by filename only — no code execution |
| Partial file selection | Newest .recrd.partial auto-selected from current directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-10 | Spoofing | Plugin directory DLL listing | accept | Phase 8 only lists filenames — no assembly loading. Phase 11 adds AssemblyLoadContext isolation. |
| T-8-11 | Tampering | .recrd.partial auto-selection | mitigate | Only scan current working directory (not recursive); validate file has .recrd.partial extension; deserialize via RecrdJsonContext (type-safe) |
| T-8-12 | Information Disclosure | Version command OS info | accept | OS description is public system info, not sensitive |
</threat_model>

<verification>
1. `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Cli.Tests/ --no-build` exits 0
2. `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet run --project apps/recrd-cli -- version` prints version, .NET, OS info
3. `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet run --project apps/recrd-cli -- plugins list` prints "No plugins installed"
4. `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet run --project apps/recrd-cli -- recover` prints error about no .recrd.partial files
5. `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet run --project apps/recrd-cli -- --help` lists all 11 commands
</verification>

<success_criteria>
- Version, recover, plugins commands fully implemented
- All 26+ CLI tests pass (TDD green)
- Coverage gate >= 90% for recrd-cli
- PublishReadyToRun=true in csproj
- Full build exits 0: `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build recrd.sln --no-restore`
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-polish/08-04-SUMMARY.md`
</output>
