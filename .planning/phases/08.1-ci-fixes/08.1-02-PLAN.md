---
phase: 08.1-ci-fixes
plan: 02
type: execute
wave: 1
depends_on: [08.1-01]
files_modified: [.github/workflows/ci.yml]
autonomous: true
requirements: [CI-01]

must_haves:
  truths:
    - "CI workflow uses Node.js v22"
    - "Playwright browser installation uses the .sh wrapper in output directory"
    - "CLAUDE.md and GEMINI.md are consistent with ci.yml for playwright.sh usage"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline configuration"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "packages/Recrd.Recording/bin/Debug/net10.0/playwright.sh"
      via: "bash execution"
---

<objective>
Update the CI pipeline to use Node.js v22 and ensure the Playwright browser installation step is correctly configured to use the playwright.sh wrapper. Verify documentation consistency.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.github/workflows/ci.yml
@packages/Recrd.Recording/Recrd.Recording.csproj
@packages/Recrd.Recording/playwright.sh
@CLAUDE.md
@GEMINI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Node.js version in ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Modify `.github/workflows/ci.yml` to update the `node-version` from `'20'` to `'22'` in the `Setup Node.js (for rfbrowser init)` step.
  </action>
  <verify>
    <automated>grep "node-version: '22'" .github/workflows/ci.yml</automated>
  </verify>
  <done>Node.js version updated to 22 in CI configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Playwright installation step and documentation consistency</name>
  <files>.github/workflows/ci.yml, CLAUDE.md, GEMINI.md</files>
  <action>
    Confirm that `Install Playwright browsers (for Recording tests)` in `.github/workflows/ci.yml` uses the command:
    `bash packages/Recrd.Recording/bin/Debug/net10.0/playwright.sh install chromium`.
    Ensure `CLAUDE.md` and `GEMINI.md` use the exact same path for the playwright installation command.
    The current command in ci.yml is already correct, so this task is mainly about verifying and ensuring no regressions while documentation is checked.
  </action>
  <verify>
    <automated>grep "bash packages/Recrd.Recording/bin/Debug/net10.0/playwright.sh install chromium" .github/workflows/ci.yml CLAUDE.md GEMINI.md</automated>
  </verify>
  <done>Playwright installation command verified and consistent across CI and documentation.</done>
</task>

<task type="auto">
  <name>Task 3: Commit changes</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Commit the changes to the repository with a descriptive message: "ci: update Node.js to 22 and ensure playwright.sh usage".
  </action>
  <verify>
    <automated>git diff --exit-code .github/workflows/ci.yml || echo "Changes detected (expected if not yet committed)"</automated>
  </verify>
  <done>Changes committed to git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Runner → External | CI runner fetches Node.js 22 from official actions/setup-node. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01 | Tampering | ci.yml | mitigate | Use official GitHub Actions (actions/setup-node@v4) and pinned versions where possible. |
</threat_model>

<verification>
Check that `.github/workflows/ci.yml` contains `node-version: '22'` and the playwright installation step.
</verification>

<success_criteria>
- CI configuration is updated.
- Documentation matches CI commands.
- Changes are committed.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-ci-fixes/08.1-02-SUMMARY.md`
</output>
