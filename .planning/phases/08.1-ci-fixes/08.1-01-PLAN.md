---
phase: 08.1-ci-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/Recrd.Integration.Tests/Recrd.Integration.Tests.csproj, .github/workflows/ci.yml]
autonomous: true
requirements: [CI-01, COMP-10]

must_haves:
  truths:
    - "Integration test project builds without NETSDK1086 warning"
    - "CI workflow installs browsers before the first test execution"
  artifacts:
    - path: "tests/Recrd.Integration.Tests/Recrd.Integration.Tests.csproj"
      provides: "Integration test project configuration"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline definition"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/Recrd.Recording.Tests/Recrd.Recording.Tests.csproj"
      via: "Playwright browser installation"
---

<objective>
Fix CI failures and build warnings by removing redundant framework references and ensuring Playwright browsers are available for all test steps.

Purpose: Prevent CI failures in Recording tests and remove noise from local builds.
Output: Clean build and corrected CI workflow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/Recrd.Integration.Tests/Recrd.Integration.Tests.csproj
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant FrameworkReference in Integration Tests</name>
  <files>tests/Recrd.Integration.Tests/Recrd.Integration.Tests.csproj</files>
  <action>
    Remove the `<FrameworkReference Include="Microsoft.AspNetCore.App" />` line from the project file.
    The `Sdk="Microsoft.NET.Sdk.Web"` attribute already includes this reference, and having it explicitly causes a NETSDK1086 warning.
  </action>
  <verify>
    <automated>dotnet build tests/Recrd.Integration.Tests/Recrd.Integration.Tests.csproj | grep -v "NETSDK1086"</automated>
  </verify>
  <done>Project builds without NETSDK1086 warning.</done>
</task>

<task type="auto">
  <name>Task 2: Reorder Playwright installation in CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Move the `Install Playwright browsers (for Recording tests)` step to be immediately BEFORE the first `Test` step.
    This ensures that when `dotnet test --no-build --filter "Category!=Integration"` runs, the Chromium browser is already installed for the `Recrd.Recording.Tests` project.
  </action>
  <verify>
    <automated>grep -n "Install Playwright browsers" .github/workflows/ci.yml | cut -d: -f1 | xargs -I {} bash -c "grep -n \"name: Test\" .github/workflows/ci.yml | head -n 1 | cut -d: -f1 | xargs -I [] [ {} -lt [] ] && echo 'Correctly ordered' || (echo 'Incorrectly ordered' && exit 1)"</automated>
  </verify>
  <done>Playwright installation step appears before the first test step in ci.yml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Runner → Playwright | Downloads browser binaries from external Microsoft CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01 | Tampering | ci.yml | mitigate | Use official actions (actions/checkout, actions/setup-dotnet) with version pinning (v4) |
| T-08.1-02 | Denial of Service | ci.yml | accept | External CDN failure is a standard CI risk; no project-specific mitigation |
</threat_model>

<verification>
Run `dotnet build` on the solution to ensure no regressions.
</verification>

<success_criteria>
- [ ] `dotnet build tests/Recrd.Integration.Tests/Recrd.Integration.Tests.csproj` has zero warnings.
- [ ] `.github/workflows/ci.yml` has Playwright installation before the first Test step.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-ci-fixes/08.1-01-SUMMARY.md`
</output>
