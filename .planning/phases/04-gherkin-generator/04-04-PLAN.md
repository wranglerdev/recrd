---
phase: 04-gherkin-generator
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified: []
autonomous: true
requirements: [GHER-01, GHER-02, GHER-03, GHER-04, GHER-05, GHER-06, GHER-07, GHER-08, GHER-09]

must_haves:
  truths:
    - "All Gherkin tests pass (full suite green)"
    - "dotnet format passes with no violations"
    - "tdd/phase-04 branch merged to main"
    - "Full solution builds and all tests across all projects pass"
  artifacts: []
  key_links: []
---

<objective>
Green phase: verify all 5 test suites pass, run full solution test suite, check format compliance, and merge tdd/phase-04 to main. Phase 04 complete.

Purpose: Final verification and merge. Confirms all 9 GHER requirements are satisfied.
Output: Clean main branch with all Gherkin tests green.
</objective>

<execution_context>
@/home/gil/dev/recrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/gil/dev/recrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-gherkin-generator/04-CONTEXT.md
@.planning/phases/04-gherkin-generator/04-02-SUMMARY.md
@.planning/phases/04-gherkin-generator/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite verification and tdd/phase-04 merge to main</name>
  <files></files>
  <read_first>
    packages/Recrd.Gherkin/GherkinGenerator.cs,
    packages/Recrd.Gherkin/Internal/StepTextRenderer.cs,
    packages/Recrd.Gherkin/Internal/GroupingClassifier.cs,
    packages/Recrd.Gherkin/Internal/ExemplosTableBuilder.cs
  </read_first>
  <action>
1. **Run Gherkin test suite**:
   ```
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build tests/Recrd.Gherkin.Tests/Recrd.Gherkin.Tests.csproj --no-restore
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test tests/Recrd.Gherkin.Tests --no-build
   ```
   All 5 test files must pass: FixedScenarioTests, DataDrivenTests, VariableMismatchTests, GroupingTests, DeterminismTests.

2. **Run full solution test suite** (regression check):
   ```
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build recrd.sln --no-restore
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test --no-build
   ```
   All tests across Core, Data, Gherkin (and any other test projects) must pass.

3. **Format check**:
   ```
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet format --verify-no-changes
   ```
   Must exit 0. If violations found, run `dotnet format` to fix, then re-verify.

4. **Self-check**: Verify each GHER requirement has passing test coverage:
   - GHER-01: FixedScenarioTests — Cenario emitted for zero-variable session
   - GHER-02: DataDrivenTests — Esquema do Cenario + Exemplos table
   - GHER-03: VariableMismatchTests — GherkinException on missing variable
   - GHER-04: VariableMismatchTests — stderr warning on extra column
   - GHER-05: GroupingTests — GroupStep keyword mapping
   - GHER-06: GroupingTests — default heuristic
   - GHER-07: DeterminismTests — byte-identical output
   - GHER-08: FixedScenarioTests — UTF-8 no-BOM, # language: pt header
   - GHER-09: DataDrivenTests — Exemplos column order by first appearance

5. **Merge to main**:
   ```
   git checkout main
   git merge tdd/phase-04 --no-ff -m "feat(04): merge tdd/phase-04 — Gherkin generator complete, all tests green"
   ```

6. **Post-merge verification**:
   ```
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test --no-build
   ```
   All tests still pass on main.
  </action>
  <verify>
    <automated>DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test --no-build && DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet format --verify-no-changes</automated>
  </verify>
  <acceptance_criteria>
    - dotnet test (full suite) exits 0 — all tests green
    - dotnet format --verify-no-changes exits 0 — no formatting violations
    - git branch --show-current returns `main`
    - git log --oneline -1 contains `tdd/phase-04`
    - FixedScenarioTests, DataDrivenTests, VariableMismatchTests, GroupingTests, DeterminismTests all present and passing
  </acceptance_criteria>
  <done>All GHER-01 through GHER-09 tests pass. Full solution green. Format clean. tdd/phase-04 merged to main. Phase 04 complete.</done>
</task>

</tasks>

<verification>
- `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test --no-build` exits 0
- `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet format --verify-no-changes` exits 0
- `git branch --show-current` returns `main`
</verification>

<success_criteria>
- All tests across all projects pass
- Format check passes
- tdd/phase-04 branch merged to main
- All 9 GHER requirements have verified test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/04-gherkin-generator/04-04-SUMMARY.md`
</output>
