---
phase: 03-data-providers
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "All Recrd.Data.Tests pass green"
    - "Full solution builds clean"
    - "tdd/phase-03 branch merged to main"
    - "No NotImplementedException anywhere in Recrd.Data"
  artifacts: []
  key_links:
    - from: "packages/Recrd.Data/CsvDataProvider.cs"
      to: "packages/Recrd.Core/Interfaces/IDataProvider.cs"
      via: "implements IDataProvider.StreamAsync"
      pattern: "IAsyncEnumerable.*StreamAsync"
    - from: "packages/Recrd.Data/JsonDataProvider.cs"
      to: "packages/Recrd.Core/Interfaces/IDataProvider.cs"
      via: "implements IDataProvider.StreamAsync"
      pattern: "IAsyncEnumerable.*StreamAsync"
---

<objective>
Green phase: merge tdd/phase-03 to main with all tests passing, full solution build verified.

Purpose: Close the TDD red-green cycle. Confirm all DATA requirements are satisfied and the solution is clean for downstream phases.
Output: Clean main branch with all Phase 3 code merged and tests green.
</objective>

<execution_context>
@/home/gil/dev/recrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/gil/dev/recrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-providers/03-CONTEXT.md
@.planning/phases/03-data-providers/03-02-SUMMARY.md
@.planning/phases/03-data-providers/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify all tests green</name>
  <files></files>
  <read_first>
    packages/Recrd.Data/CsvDataProvider.cs,
    packages/Recrd.Data/JsonDataProvider.cs,
    packages/Recrd.Data/DataParseException.cs
  </read_first>
  <action>
1. Build the entire solution:
   ```bash
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build recrd.sln
   ```

2. Run ALL tests (not just Recrd.Data.Tests — ensure no regressions in Core tests):
   ```bash
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test recrd.sln --no-build
   ```

3. Run format check:
   ```bash
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet format --verify-no-changes
   ```

4. If format issues exist, fix them:
   ```bash
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet format
   ```
   Then rebuild and retest.

5. Verify no `NotImplementedException` remains in Recrd.Data source files:
   ```bash
   grep -r "NotImplementedException" packages/Recrd.Data/ --include="*.cs"
   ```
   This must return zero results.

6. Merge `tdd/phase-03` to `main`:
   ```bash
   git checkout main
   git merge tdd/phase-03 --no-ff -m "feat(03): merge data providers — CsvDataProvider + JsonDataProvider"
   ```

7. Delete the TDD branch:
   ```bash
   git branch -d tdd/phase-03
   ```

8. Final verification on main:
   ```bash
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test recrd.sln --no-build
   ```
  </action>
  <verify>
    <automated>DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test recrd.sln --no-build</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build recrd.sln` exits 0
    - `dotnet test recrd.sln --no-build` exits 0 with all tests passing
    - `dotnet format --verify-no-changes` exits 0
    - `grep -r "NotImplementedException" packages/Recrd.Data/ --include="*.cs"` returns no results
    - Current branch is `main`
    - `git log --oneline -1` contains `feat(03)` or equivalent merge commit
  </acceptance_criteria>
  <done>All tests green on main. tdd/phase-03 merged. Solution builds clean. No NotImplementedException in Recrd.Data. Format check passes.</done>
</task>

</tasks>

<verification>
- `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet test recrd.sln --no-build` — all green (Core + Data)
- `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet format --verify-no-changes` — clean
- Branch is `main`
- No NotImplementedException in packages/Recrd.Data/
</verification>

<success_criteria>
- Full solution builds and all tests pass on main
- Format check passes
- TDD branch merged and deleted
- Phase 3 complete: DATA-01 through DATA-05 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-providers/03-04-SUMMARY.md`
</output>
