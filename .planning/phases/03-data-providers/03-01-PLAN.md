---
phase: 03-data-providers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/Recrd.Data/DataParseException.cs
  - packages/Recrd.Data/CsvDataProvider.cs
  - packages/Recrd.Data/JsonDataProvider.cs
  - packages/Recrd.Data/Recrd.Data.csproj
  - tests/Recrd.Data.Tests/CsvDataProviderTests.cs
  - tests/Recrd.Data.Tests/JsonDataProviderTests.cs
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "All test files compile and are discoverable by xUnit"
    - "Every test fails (red phase — no implementation yet)"
    - "DataParseException has LineNumber, OffendingLine, FilePath properties"
    - "CsvHelper 33.1.0 is referenced in Recrd.Data.csproj"
  artifacts:
    - path: "packages/Recrd.Data/DataParseException.cs"
      provides: "Typed exception with diagnostic properties"
      contains: "public sealed class DataParseException"
    - path: "packages/Recrd.Data/CsvDataProvider.cs"
      provides: "Stub CSV provider implementing IDataProvider"
      contains: "class CsvDataProvider : IDataProvider"
    - path: "packages/Recrd.Data/JsonDataProvider.cs"
      provides: "Stub JSON provider implementing IDataProvider"
      contains: "class JsonDataProvider : IDataProvider"
    - path: "tests/Recrd.Data.Tests/CsvDataProviderTests.cs"
      provides: "Red test suite for DATA-01, DATA-02, DATA-03"
      min_lines: 80
    - path: "tests/Recrd.Data.Tests/JsonDataProviderTests.cs"
      provides: "Red test suite for DATA-04, DATA-05"
      min_lines: 40
  key_links:
    - from: "tests/Recrd.Data.Tests/CsvDataProviderTests.cs"
      to: "packages/Recrd.Data/CsvDataProvider.cs"
      via: "new CsvDataProvider(filePath, delimiter)"
      pattern: "new CsvDataProvider"
    - from: "tests/Recrd.Data.Tests/JsonDataProviderTests.cs"
      to: "packages/Recrd.Data/JsonDataProvider.cs"
      via: "new JsonDataProvider(filePath)"
      pattern: "new JsonDataProvider"
---

<objective>
TDD Red Phase: Create all test suites, DataParseException, stub providers, and CsvHelper dependency for Phase 3 Data Providers.

Purpose: Establish the red-green TDD cycle — all tests committed failing before any implementation begins, on the `tdd/phase-03` branch per D-08.
Output: 2 test files (red), DataParseException class, 2 stub provider classes, CsvHelper PackageReference added.
</objective>

<execution_context>
@/home/gil/dev/recrd/.claude/get-shit-done/workflows/execute-plan.md
@/home/gil/dev/recrd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-providers/03-CONTEXT.md
@.planning/phases/03-data-providers/03-RESEARCH.md

<interfaces>
<!-- IDataProvider from Recrd.Core — the contract both providers must implement -->
From packages/Recrd.Core/Interfaces/IDataProvider.cs:
```csharp
namespace Recrd.Core.Interfaces;

public interface IDataProvider
{
    IAsyncEnumerable<IReadOnlyDictionary<string, string>> StreamAsync(CancellationToken cancellationToken = default);
}
```

From packages/Recrd.Data/Recrd.Data.csproj:
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyName>Recrd.Data</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\packages\Recrd.Core\Recrd.Core.csproj" />
  </ItemGroup>
</Project>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataParseException, stub providers, and add CsvHelper dependency</name>
  <files>
    packages/Recrd.Data/DataParseException.cs,
    packages/Recrd.Data/CsvDataProvider.cs,
    packages/Recrd.Data/JsonDataProvider.cs,
    packages/Recrd.Data/Recrd.Data.csproj,
    packages/Recrd.Data/Placeholder.cs
  </files>
  <read_first>
    packages/Recrd.Data/Placeholder.cs,
    packages/Recrd.Data/Recrd.Data.csproj,
    packages/Recrd.Core/Interfaces/IDataProvider.cs
  </read_first>
  <action>
1. Delete `packages/Recrd.Data/Placeholder.cs` (Phase 1 stub, no longer needed).

2. Add CsvHelper 33.1.0 to `Recrd.Data.csproj` per D-01:
   ```bash
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet add packages/Recrd.Data/Recrd.Data.csproj package CsvHelper --version 33.1.0
   ```

3. Create `packages/Recrd.Data/DataParseException.cs` per D-06 and D-07:
   ```csharp
   namespace Recrd.Data;

   public sealed class DataParseException : Exception
   {
       public int LineNumber { get; }
       public string OffendingLine { get; }
       public string FilePath { get; }

       public DataParseException(
           int lineNumber,
           string offendingLine,
           string filePath,
           string message,
           Exception? innerException = null)
           : base(message, innerException)
       {
           LineNumber = lineNumber;
           OffendingLine = offendingLine;
           FilePath = filePath;
       }
   }
   ```

4. Create `packages/Recrd.Data/CsvDataProvider.cs` as a STUB — implements `IDataProvider` but throws `NotImplementedException` from `StreamAsync`:
   ```csharp
   using System.Runtime.CompilerServices;
   using Recrd.Core.Interfaces;

   namespace Recrd.Data;

   public sealed class CsvDataProvider : IDataProvider
   {
       private readonly string _filePath;
       private readonly string _delimiter;

       public CsvDataProvider(string filePath, string delimiter = ",")
       {
           _filePath = filePath ?? throw new ArgumentNullException(nameof(filePath));
           _delimiter = delimiter ?? throw new ArgumentNullException(nameof(delimiter));
       }

       public async IAsyncEnumerable<IReadOnlyDictionary<string, string>> StreamAsync(
           [EnumeratorCancellation] CancellationToken cancellationToken = default)
       {
           // TODO: Phase 3 Plan 02 — implement CsvHelper streaming
           throw new NotImplementedException();
           yield break; // unreachable — satisfies compiler requirement for async iterator
       }
   }
   ```

5. Create `packages/Recrd.Data/JsonDataProvider.cs` as a STUB — implements `IDataProvider` but throws `NotImplementedException` from `StreamAsync`:
   ```csharp
   using System.Runtime.CompilerServices;
   using Recrd.Core.Interfaces;

   namespace Recrd.Data;

   public sealed class JsonDataProvider : IDataProvider
   {
       private readonly string _filePath;

       public JsonDataProvider(string filePath)
       {
           _filePath = filePath ?? throw new ArgumentNullException(nameof(filePath));
       }

       public async IAsyncEnumerable<IReadOnlyDictionary<string, string>> StreamAsync(
           [EnumeratorCancellation] CancellationToken cancellationToken = default)
       {
           // TODO: Phase 3 Plan 03 — implement System.Text.Json streaming
           throw new NotImplementedException();
           yield break; // unreachable — satisfies compiler requirement for async iterator
       }
   }
   ```

6. Verify build compiles:
   ```bash
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build packages/Recrd.Data/Recrd.Data.csproj --no-restore
   ```
  </action>
  <verify>
    <automated>DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build packages/Recrd.Data/Recrd.Data.csproj</automated>
  </verify>
  <acceptance_criteria>
    - packages/Recrd.Data/Placeholder.cs does NOT exist
    - packages/Recrd.Data/Recrd.Data.csproj contains `PackageReference Include="CsvHelper" Version="33.1.0"`
    - packages/Recrd.Data/DataParseException.cs contains `public sealed class DataParseException : Exception`
    - packages/Recrd.Data/DataParseException.cs contains `public int LineNumber { get; }`
    - packages/Recrd.Data/DataParseException.cs contains `public string OffendingLine { get; }`
    - packages/Recrd.Data/DataParseException.cs contains `public string FilePath { get; }`
    - packages/Recrd.Data/CsvDataProvider.cs contains `public sealed class CsvDataProvider : IDataProvider`
    - packages/Recrd.Data/CsvDataProvider.cs contains `public CsvDataProvider(string filePath, string delimiter = ",")`
    - packages/Recrd.Data/JsonDataProvider.cs contains `public sealed class JsonDataProvider : IDataProvider`
    - packages/Recrd.Data/JsonDataProvider.cs contains `public JsonDataProvider(string filePath)`
    - `dotnet build packages/Recrd.Data/Recrd.Data.csproj` exits 0
  </acceptance_criteria>
  <done>DataParseException with 3 diagnostic properties, 2 stub providers implementing IDataProvider, CsvHelper 33.1.0 referenced, project builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write all red test suites and commit on tdd/phase-03 branch</name>
  <files>
    tests/Recrd.Data.Tests/CsvDataProviderTests.cs,
    tests/Recrd.Data.Tests/JsonDataProviderTests.cs,
    tests/Recrd.Data.Tests/PlaceholderTests.cs
  </files>
  <read_first>
    tests/Recrd.Data.Tests/PlaceholderTests.cs,
    tests/Recrd.Data.Tests/Recrd.Data.Tests.csproj,
    packages/Recrd.Data/DataParseException.cs,
    packages/Recrd.Data/CsvDataProvider.cs,
    packages/Recrd.Data/JsonDataProvider.cs,
    packages/Recrd.Core/Interfaces/IDataProvider.cs
  </read_first>
  <behavior>
    CsvDataProviderTests.cs must cover:
    - DATA-01: RFC 4180 basic parsing — reads header + data rows, returns correct column names and values
    - DATA-01: BOM-tolerant — reads a UTF-8 BOM-prefixed CSV file correctly (first column name not corrupted)
    - DATA-01: Custom delimiter — reads semicolon-delimited file with `delimiter: ";"` constructor arg
    - DATA-01: Quoted fields with embedded commas — `"foo,bar"` returns as single value `foo,bar`
    - DATA-01: Quoted fields with embedded newlines — field containing `\r\n` parsed as single value
    - DATA-02: Unclosed quote throws DataParseException with LineNumber > 0 and non-empty OffendingLine
    - DATA-02: Mismatched column count (row has fewer columns than header) throws DataParseException with LineNumber
    - DATA-02: Mismatched column count (row has more columns than header) throws DataParseException with LineNumber
    - DATA-03: 50MB CSV streams with heap delta <= 100MB (GC.GetTotalMemory before/after)
    - DATA-01: Empty cell values yield empty string ("") key present in dictionary, not absent key
    - DATA-01: Cancellation token support — cancelling mid-stream stops enumeration

    JsonDataProviderTests.cs must cover:
    - DATA-04: Flat JSON array — `[{"name":"Gil","age":"30"}]` returns dictionary with keys `name`, `age`
    - DATA-04: Nested object flattening — `[{"user":{"name":"Gil"}}]` returns key `user.name`
    - DATA-04: Deeply nested — `[{"a":{"b":{"c":"v"}}}]` returns key `a.b.c`
    - DATA-04: Array field silently skipped per D-04 — `[{"tags":["a","b"],"name":"x"}]` returns only `name`
    - DATA-04: Mixed nested and flat — object with both flat and nested keys returns correct combined dictionary
    - DATA-05: Non-array root (object `{}`) throws DataParseException with message containing "root" and "array"
    - DATA-05: Non-array root (string `"hello"`) throws DataParseException
    - DATA-04: Null value in JSON renders as empty string
    - DATA-04: Boolean and number values render as their ToString() representation
  </behavior>
  <action>
1. Create branch `tdd/phase-03` from `main`:
   ```bash
   git checkout -b tdd/phase-03
   ```

2. Delete `tests/Recrd.Data.Tests/PlaceholderTests.cs`.

3. Create `tests/Recrd.Data.Tests/CsvDataProviderTests.cs` with the full test suite from the behavior list above. All tests should:
   - Use `[Fact]` for single-case tests and `[Theory]` + `[InlineData]` where appropriate
   - Create temporary CSV files in the test using `Path.GetTempFileName()` with try/finally cleanup
   - Instantiate `new CsvDataProvider(filePath)` or `new CsvDataProvider(filePath, ";")` for delimiter tests
   - Call `provider.StreamAsync()` via `await foreach` and collect results into a `List<IReadOnlyDictionary<string,string>>`
   - Assert on dictionary keys and values using `Assert.Equal`
   - For DATA-02: use `await Assert.ThrowsAsync<DataParseException>(async () => { await foreach (var _ in provider.StreamAsync()) { } })`
   - For DATA-03 (50MB memory test): Generate a ~50MB temp CSV with header `col1,col2,col3` and 500,000 rows of `val{i},val{i},val{i}`. Call `GC.Collect(2, GCCollectionMode.Aggressive, blocking: true)` before measuring `GC.GetTotalMemory(true)` both before and after streaming. Assert `after - before < 100 * 1024 * 1024`.
   - For cancellation test: Create a `CancellationTokenSource`, cancel it after reading 1 row, assert enumeration stops

4. Create `tests/Recrd.Data.Tests/JsonDataProviderTests.cs` with the full test suite from the behavior list above. All tests should:
   - Create temporary JSON files using `Path.GetTempFileName()` with try/finally cleanup
   - Write JSON content via `File.WriteAllTextAsync`
   - Instantiate `new JsonDataProvider(filePath)`
   - Call `provider.StreamAsync()` via `await foreach` and collect results
   - For DATA-05: use `await Assert.ThrowsAsync<DataParseException>(async () => { await foreach (var _ in provider.StreamAsync()) { } })`
   - Assert exception message contains "root" and "array" (case-insensitive)

5. Verify tests compile but fail:
   ```bash
   DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build tests/Recrd.Data.Tests/Recrd.Data.Tests.csproj
   ```
   Tests should compile (stubs exist) but all fail at runtime with NotImplementedException.

6. Commit all changes on `tdd/phase-03` branch:
   ```
   test(03-01): add failing test suites for CsvDataProvider and JsonDataProvider
   ```
  </action>
  <verify>
    <automated>DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet build tests/Recrd.Data.Tests/Recrd.Data.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - tests/Recrd.Data.Tests/PlaceholderTests.cs does NOT exist
    - tests/Recrd.Data.Tests/CsvDataProviderTests.cs contains `class CsvDataProviderTests`
    - tests/Recrd.Data.Tests/CsvDataProviderTests.cs contains `DataParseException`
    - tests/Recrd.Data.Tests/CsvDataProviderTests.cs contains `GC.GetTotalMemory`
    - tests/Recrd.Data.Tests/CsvDataProviderTests.cs contains at least 8 `[Fact]` or `[Theory]` attributes
    - tests/Recrd.Data.Tests/JsonDataProviderTests.cs contains `class JsonDataProviderTests`
    - tests/Recrd.Data.Tests/JsonDataProviderTests.cs contains `user.name`
    - tests/Recrd.Data.Tests/JsonDataProviderTests.cs contains `DataParseException`
    - tests/Recrd.Data.Tests/JsonDataProviderTests.cs contains at least 6 `[Fact]` or `[Theory]` attributes
    - `dotnet build tests/Recrd.Data.Tests/Recrd.Data.Tests.csproj` exits 0
    - Current branch is `tdd/phase-03`
    - Most recent commit message contains `test(03-01)`
  </acceptance_criteria>
  <done>All test suites committed red on tdd/phase-03. Tests compile but fail with NotImplementedException. PlaceholderTests.cs deleted.</done>
</task>

</tasks>

<verification>
- `dotnet build tests/Recrd.Data.Tests/Recrd.Data.Tests.csproj` exits 0 (compiles)
- `dotnet test tests/Recrd.Data.Tests/Recrd.Data.Tests.csproj` shows all tests FAILING (red phase)
- Branch is `tdd/phase-03`
- CsvHelper 33.1.0 in Recrd.Data.csproj
</verification>

<success_criteria>
- DataParseException exists with LineNumber, OffendingLine, FilePath properties
- CsvDataProvider and JsonDataProvider stubs implement IDataProvider
- All test suites compile and are discoverable by xUnit
- All tests fail (red) — no implementation yet
- All files committed on tdd/phase-03 branch
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-providers/03-01-SUMMARY.md`
</output>
