# Quick Task 260410-16w: fix the complete flow of the CI, test and publish via release on github - Context

**Gathered:** 2026-04-10
**Status:** Ready for planning

<domain>
## Task Boundary

Fix the CI pipeline (test failures) and implement a working GitHub Release + NuGet publish workflow. The user reported that the current flow "really never worked".

</domain>

<decisions>
## Implementation Decisions

### CI Failure (TestFixtures)
- Fix: Add explicit `dotnet restore tests/Recrd.Cli.Tests/Plugins/TestFixtures/FakePlugin.csproj` to `ci.yml` and `publish.yml` (or the new unified workflow). This project is not in the solution and was causing build failures in CI.

### Workflow Consolidation
- Replace `publish.yml` with a unified `release.yml` (or similar name) that handles:
  - NuGet publish (to GitHub Packages).
  - GitHub Release creation.
  - Attachment of self-contained binaries for all target platforms (as hinted in Phase 09 goals).
  
### Release Triggers
- Use `v*` tags for releases.
- Maintain the "main branch only" guard for publishing, but use a more robust check than `github.event.base_ref`.

### Claude's Discretion
- Choosing `softprops/action-gh-release` for GitHub Releases as it is the industry standard.
- Using a matrix build for cross-platform binaries to be attached to the release.
- Consolidating "Release" configuration across all steps in the publish workflow.

</decisions>

<specifics>
## Specific Ideas

- The user provided a failing run URL which points to missing `project.assets.json` for `FakePlugin.csproj`.
- `ci.yml` needs to be updated to ensure this fixture project is restored before `dotnet test` runs.

</specifics>

<canonical_refs>
## Canonical References

- [Failing run URL](https://github.com/wranglerdev/recrd/actions/runs/24225303102/job/70725136473?pr=10)
- `ROADMAP.md` Phase 09 and Phase 05 goals.

</canonical_refs>
