# Quick Task Plan: 260410-16w - Fix CI and Unified Release

Fix the CI pipeline test failures and implement a robust, unified GitHub Release + NuGet publish workflow.

## must_haves
- [ ] `ci.yml` passes in GitHub Actions (no missing `project.assets.json`).
- [ ] `release.yml` replaces `publish.yml` and triggers on `v*` tags.
- [ ] `release.yml` creates a GitHub Release with self-contained binaries for win/linux/macos.
- [ ] `release.yml` pushes NuGet packages to GitHub Packages.
- [ ] All workflows use `DOTNET_SYSTEM_NET_DISABLEIPV6=1`.

## User review required
> [!IMPORTANT]
> This plan modifies CI/CD workflows which are difficult to test locally. Manual trigger or a dummy tag push may be needed to verify the "Release" part.

- **Choice:** Consolidate `publish.yml` into a more powerful `release.yml`.
- **Reasoning:** User reported the flow "never worked". A fresh, standard implementation of a multi-artifact release is better than patching the minimal `publish.yml`.

- **Choice:** Use `softprops/action-gh-release`.
- **Reasoning:** Industry standard for reliable GitHub releases.

---

## Tasks

### 1. Fix CI TestFixtures restore
- **files:** `.github/workflows/ci.yml`
- **action:** Add a step to explicitly restore `tests/Recrd.Cli.Tests/Plugins/TestFixtures/FakePlugin.csproj`.
- **verify:** `DOTNET_SYSTEM_NET_DISABLEIPV6=1 dotnet restore` followed by building the solution locally should work (this is already true, but CI fails because it doesn't build the solution with all dependencies).
- **done:** CI step added.

### 2. Create Unified Release Workflow
- **files:** `.github/workflows/release.yml`, `.github/workflows/publish.yml` (delete)
- **action:** 
  - Create `release.yml` with a build matrix for `win-x64`, `linux-x64`, `osx-x64`, `osx-arm64`.
  - Implement `dotnet publish` with `--self-contained true` and `-p:PublishSingleFile=true`.
  - Add NuGet pack and push to GitHub Packages.
  - Add GitHub Release creation and asset upload.
  - Fix the "main branch only" tag guard.
- **verify:** Check YAML syntax and logic.
- **done:** `release.yml` created and `publish.yml` removed.

### 3. Verification & STATE.md Update
- **files:** `.planning/STATE.md`
- **action:** Update "Quick Tasks Completed" and "Last activity".
- **verify:** `STATE.md` reflects the changes.
- **done:** STATE.md updated.
